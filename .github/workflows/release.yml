name: Release

on:
  push:
    tags:
      - "[0-9]+\\.[0-9]+\\.[0-9]+"

jobs:
  call:
    uses: org/ci-workflows/.github/workflows/release-with-ai-notes.yml@v1
    with:
      tag: ${{ github.ref_name }}
      model: openai/gpt-4o
      audience_language: es
